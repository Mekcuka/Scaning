---
slug: map-2d
title: Карта — режимы 2D, Площадки и 3D
tags: [map, ui, layers, infrastructure, footprints, earthwork]
tab_hints: [map]
roles: [viewer, analyst, admin, data_manager]
summary: Панель слоёв, объекты инфраструктуры, POI; три режима карты — иконки 2D, контуры площадок, просмотр 3D.
---

# Карта — режимы 2D, Площадки и 3D

Раздел **Карта** (`/{id-проекта}/map`) показывает объекты инфраструктуры и POI активного проекта. Id проекта виден в адресной строке; при смене проекта в меню URL обновляется.

## Переключатель режимов

На панели инструментов — три иконки:

| Режим | Движок | Назначение |
|-------|--------|------------|
| **2D** | OpenLayers | Редактирование: точки, линии, линейка, вставка |
| **Площадки** | OpenLayers | Контуры площадок вместо иконок (см. ниже); редактирование как в 2D |
| **3D** | MapLibre + Three.js | Только просмотр (модели, трубы, рельеф); рисование недоступно |

По умолчанию при открытии карты — **2D**. Режим **3D** требует `VITE_MAP_3D_ENABLED=true` в сборке frontend.

## Режим «Площадки» (контуры)

Earthwork-eligible **точечные** объекты (все подтипы «Точка», кроме **Узел** — в том числе **Карьер песка**, ГКС, кусты, ПС и др.) рисуются **полигонами** площадок:

- если задана схема в «Схема…» — контур из земляных работ;
- иначе — прямоугольник **120×80 м** (или сохранённые L/W и поворот).

**Линии**, **POI** и **узлы** — без изменений (как в 2D). Клик по контуру выбирает объект; перетаскивание якоря сдвигает площадку.

### Точки подключения линий

Если у площадки заданы **точки подключения** (`footprint_line_connections`), концы линейных объектов, привязанных к этой точке, **визуально** сходятся к выбранному ребру контура (или остаются в центре). Координаты в базе не меняются.

- **На объекте:** карточка earthwork-точки → вкладка «Основное» (только в режиме «Площадки»): сторона света, позиция вдоль ребра, **Применить шаблон проекта**.
- **На проекте:** **Параметры → Точки подключения** — шаблон по типам линий и массовое применение ко всем площадкам. Подробнее: [parameters-footprint-connections.md](parameters-footprint-connections.md).

Подробнее: [pad-earthwork-volumes.md](pad-earthwork-volumes.md), техническая спецификация — [pad-earthwork.md](../../features/pad-earthwork/pad-earthwork.md).

## Панель «Слои»

Слева (или в боковой панели) — переключатели:

- **Подложка** — спутник, схема и т.д.
- **Группы подтипов** — дороги, трубопроводы, площадки и др.
- **POI** — точки интереса (скважины, площадки).
- **Радиусы** — зоны вокруг POI для анализа.
- **3D-модели** — только в режиме **3D**.

Настройки видимости слоёв **сохраняются в браузере** на проект (localStorage). Видимость импортированных источников из внешних систем — в базе данных (чекбоксы «Источники»).

## Объекты на карте

- **Точки** — клик открывает карточку объекта.
- **Линии** — трубопроводы, дороги; в режиме редактирования можно двигать вершины. У линейных объектов (кроме забоя ГС) — вкладка **Профиль** с таблицей отметок после **Рассчитать профиль** (меню **Расчёт** на панели инструментов). См. [line-elevation-profile.md](line-elevation-profile.md).
- **Контуры площадок** — в режиме «Площадки».

При большом числе объектов карта подгружает данные по видимой области (bbox), чтобы не тормозить.

## Забои скважин и доп.стволы

На **2D-карте** можно рисовать объекты **«Забой»** (ННБ, ГС) и привязывать их к кусту. Это цели бурения для расчёта траекторий на странице **«Кустование»**.

### Основной забой и доп.ствол

Цепочка **Куст → основной забой → доп.ствол**:

| Роль | Как создать | Связь с кустом |
|------|-------------|----------------|
| **Основной** (по умолчанию) | «Забой» → ННБ или ГС | Прямая привязка к кусту |
| **Доп.ствол** | «Забой» → **Доп.ствол (ННБ/ГС)** или поле «Роль» в карточке | Через **родительский** основной забой; куст и номер скважины наследуются |

- **Основной забой** участвует в «Рассчитать до забоев» (основной ствол welleng).
- **Доп.ствол** — отдельная цель на карте (фиолетовый маркер); для проектирования боковой ветки используйте **Кустование → PyWellGeo → До забоя**.

### Линии на карте

| Линия | Когда видна |
|-------|-------------|
| Пунктир **устье → основной забой** | До полного проектирования траектории |
| Пунктир **основной → доп.ствол** (фиолетовый) | Пока **нет** рассчитанной ветки PyWellGeo до этого доп.ствола |
| Линия **ветки PyWellGeo** | После расчёта бокового ствола «До забоя» на «Кустовании» |

Когда ветка PyWellGeo уже построена, фиолетовый пунктир parent→lateral **исчезает** — связь показывает линия траектории ветки.

Подробнее: [well-trajectory.md](../../features/well-trajectory/well-trajectory.md) §2c, [map-objects-and-spatial-calculations.md](../../features/map/map-objects-and-spatial-calculations.md) §1.4.5.

## POI

POI — ключевые точки для расчёта стоимости и матрицы. Выбор POI на карте влияет на контекст AI-помощника и вкладки «Потоки» / «Матрица».

## Режим редактирования

Роли с правом записи могут менять геометрию и метаданные объектов в **2D** и **Площадки**. Опасные массовые операции (удаление) требуют подтверждения.

## 3D

Custom GLB загружаются через **Импорт 3D**. Подробнее: [import-3d.md](import-3d.md), [map-3d-features.md](../../features/map/map-3d-features.md).

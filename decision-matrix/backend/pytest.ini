[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    demo_db: tests that require persistent SQLite data/sppr.db
    pywellgeo: tests requiring pywellgeo package
addopts = --ignore=tests/test_demo_users.py
